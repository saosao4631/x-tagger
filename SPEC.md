# X Tagger 仕様書

**X(旧Twitter)のタイムラインを、自分好みに整理するChrome拡張機能**

パソコンのChromeブラウザに入れて使います。スマホアプリのXには効きません(Androidなら Firefox / Kiwi Browser に入れれば使えます)。

---

## 1. できること

### ① 広告を消す

タイムラインに混ざってくる「プロモーション」(広告ツイート)を自動で見えなくします。

### ② ツイートにタグを付ける

自分で登録した単語(タグ)がツイートの本文に入っていたら、そのツイートの上に**メルカリの検索画面のような丸いチップ**を表示し、背景を薄い青色にします。

例:「Python」というタグを登録しておくと、Pythonの話をしているツイートに `Python` チップが付きます。

**同義語**: 同じ意味の言葉をまとめられます。「Python」タグに「パイソン」「python3」を同義語として登録すると、どの書き方のツイートにも同じ `Python` チップが1つだけ付きます(3つ並んだりしません)。

**除外語**: 「猫の話は見たいけど、猫背の話は関係ない」というとき、「猫」タグに除外語「猫背」を設定できます。「猫背」としか書いていないツイートには反応しなくなります。

### ③ よく流れてくる単語を自動でタグにする

**このセットの登録タグ・自動タグにヒットしたツイート**の単語を裏で数えていて、**設定した回数(初期値: 20回)以上出てきた単語は、自動でタグに昇格**します。自動タグはグレーのチップで表示されるので、自分で登録した青いタグと区別できます。

例えば「Python」タグから頻出語「Django」が見つかり、登録・自動昇格したら、**Pythonを含まないDjangoの投稿も表示・集計対象**になります。そこから「Celery」など次の関連語が見つかり、対象が広がっていきます。どの登録タグ・自動タグにも一致しない投稿は集計しません。

登録・昇格のたびに、フィルタで非表示にしていた投稿も再判定します。対象は**Xがページ内に読み込んだ投稿**で、スクロールで新たに読み込まれた投稿にも適用されます。X全体への検索や未読み込み投稿の取得は行いません。

「最近この単語よく見るな」を拡張機能が先に気づいてくれる、という機能です。いらない自動タグは×で消せて、一度消したものは二度と自動で復活しません。

※ 登録タグ・自動タグがどちらもない場合は集計しません。最初に興味のあるタグを1つ登録してください。

### ④ タグセットの切り替え

タグの一覧を「セット」として複数保存し、ワンクリックで切り替えられます。例えば「趣味セット」「仕事セット」「推し活セット」を作っておいて、気分に合わせてタイムラインの見え方を変える、という使い方ができます。

- タグの追加・編集は、いま選んでいるセットに自動で保存されます(保存ボタン不要)
- セット名は**ダブルクリック**でその場で変更できます
- セットを切り替えると、タイムラインのチップ表示・フィルタも即座に切り替わります
- 頻出単語のカウント・自動タグ・頻出単語の除外リストは**セット別**に保存し、切り替えるとそのセットのランキングに戻ります
- タグの追加・削除や同義語・除外語の変更で判定条件が変わると、そのセットの集計と自動タグを最初から作り直します。セット名だけの変更では集計を保ちます
- 「このセットの集計・自動タグをリセット」で、選択中のセットだけを初期化できます。登録タグと頻出単語の除外リストは保持し、開いているXタブの古い集計も再保存されません

※ この修正版への更新後、以前の共通集計・自動タグは新しいランキングに引き継がず、セット別に集計を始めます。登録済みタグと手動で除外した語は引き継ぎます。

### ⑤ AIによるお手伝い(Chrome内蔵AI・対応マシンのみ)

Chromeに内蔵されたAI(Gemini Nano)を使った補助機能が2つあります。**APIキー不要・費用ゼロ**で、モデルのダウンロード後は**データが一切外部に送信されません**(AIの処理はすべて自分のパソコンの中で完結します)。

- **同義語・除外語の候補生成**: タグ編集パネルの「AIで候補を出す」を押すと、タグ名から表記ゆれ(Python→パイソン、python3)や誤ヒットしそうな語(猫→猫背)の候補をAIが提案。**候補はチェック式で表示され、ユーザーが選んだものだけ反映される**(AIが勝手に設定を変えることはない)
- **頻出単語のノイズ除去**: 「AIでノイズ除去」を押すと、AIが選んだ語をそのセットの除外リストへまとめて移動します。ボタン1回で除外まで完了し、何語・どの語を除外したかを結果に表示します。処理中にセット・集計条件を変えたりリセットした場合、古い結果は反映しません

ノイズ判定には現在のタグの文脈と、除外する語・残す語の具体例を使います。`of / at / to` などの機能語は集計側でも除外し、過去のカウント・自動タグから掃除します。2文字でも `AI / UI / IT / OS / DB` など意味のある略語は残します。`figma / issue / md` や「設計」「性能」などは、タグの話題との関係をAIが判断します。

対応していないマシン(ストレージ空き22GB未満、GPU/メモリ不足など)ではAIボタン自体が表示されず、他の機能は通常通り使えます。初回利用時にAIモデル(数GB)のダウンロードが必要です。

### ⑥ タグに合うツイートだけ表示する(フィルタモード)

設定でONにすると、ホームタイムラインで**どのタグにも当てはまらないツイートを丸ごと非表示**にします。興味のある話題だけが流れるタイムラインになります。

ただし、タグのヒット率が低いと画面がスカスカになるので、初期設定ではOFFです。プロフィールページやツイート詳細ページでは動作しません(そこまで隠すと使いものにならないため)。

---

## 2. 使い方

### インストール

1. Chromeで `chrome://extensions` を開く
2. 右上の「デベロッパーモード」をON
3. 「パッケージ化されていない拡張機能を読み込む」→ このフォルダを選択

### タグの登録

1. ツールバーのパズルピースアイコン → **X Tagger** をクリック
2. 入力欄に単語を入れて「追加」
3. チップの**文字部分をクリック**すると、同義語・除外語を編集するパネルが開く
4. チップの「×」で削除

### 設定項目

| 設定 | 初期値 | 説明 |
|---|---|---|
| 広告(プロモーション)を非表示 | ON | 広告ツイートを隠す |
| タグに一致しないツイートを非表示 | OFF | フィルタモード(ホームTLのみ) |
| 頻出単語の自動タグ化 | ON | よく出る単語を自動でタグにする |
| 自動タグ化のしきい値 | 20回 | 何回出てきたら自動タグにするか |

ポップアップには「頻出単語 上位」ランキングも表示され、「＋タグに追加」で手動でタグにできます。単語の横の「×」を押すと、そのセットのランキングから除外され、自動タグにも昇格しなくなります。他のセットには影響しません。

---

## 3. 仕組み(JSが分からない人向け)

### 拡張機能って何をしているの?

ブラウザ拡張機能は、**Webページに常駐するお手伝いさん**のようなものです。X Taggerはx.comのページが開くと一緒に起動して、画面に表示されるツイートを1つずつチェックし、「広告なら隠す」「タグの単語が入っていたらチップを貼る」という作業をし続けます。

### どうやってツイートを見張っているの?

Xのタイムラインは、スクロールするたびに新しいツイートが画面に追加されていきます。X Taggerは **MutationObserver**(ミューテーション・オブザーバー)という「ページの変化を知らせてくれる見張り役」を使って、新しいツイートが現れた瞬間に検査しています。

### 「同じツイートを二重に処理しない」工夫

Xは表示を軽くするため、**画面の外に出たツイートの入れ物を、別のツイートに使い回す**ことがあります(仮想スクロールと呼ばれる仕組み)。何も対策しないと「処理したはずのツイートが未処理に見える」「同じツイートを何度も数えてしまう」といった事故が起きます。

X Taggerは各ツイートの固有URL(`x.com/〇〇/status/数字`)を名札として記録し、

- 入れ物が使い回されて**中身が変わった**ら → 貼ってあった古いチップを剥がして検査し直す
- 一度**単語を数えた**ツイートは → スクロールで再表示されても二度と数えない

という2段構えで対処しています。

### 単語はどうやって数えているの?

日本語は英語と違って単語の間にスペースがないので、「どこからどこまでが1つの単語か」を機械が判断するのは実は難しい問題です。X TaggerはChromeに内蔵されている **Intl.Segmenter** という日本語分割機能を使っています(追加のソフトは不要)。

なお、数える対象は**登録タグ・自動タグのいずれかにヒットしたツイート**です。頻出語が登録・昇格するたびに、元のタグを含まない関連投稿へも集計範囲が広がります。そのうえで、**名詞だけ**を数えるために以下は数えません:

- URL、@メンション、ハッシュタグ記号
- 「今日」「自分」など、頻出だが意味の薄い言葉(ストップワード)
- **ひらがなを含む言葉すべて**。日本語の動詞・形容詞・助詞は「食べ**る**」「楽し**い**」「**から**」のようにほぼ必ずひらがなを含むため、漢字・カタカナ・英数字だけの言葉(猫、機械学習、ゲーム、Python など)に絞ると、残るのはほぼ名詞になります。※副作用として「こども」「いちご」のようなひらがな表記の名詞も数えられなくなります

### タグの判定の細かい話

- 大文字/小文字、全角/半角は区別しません(「PYTHON」も「python」も同じ扱い)
- 英数字の単語は**前方一致**です。「run」タグは running / runner にもヒットします(活用形をまとめて拾うため)。一方「cpython」のように別の単語の途中にある場合はヒットしません。「cat」で catch が引っかかるような不要ヒットは、除外語に「catch」を追加して弾きます
- 日本語は単語の区切りが機械的に判定できないため部分一致です。誤反応する場合は同じく除外語で調整します(例:「猫」に対する「猫背」)

### データはどこに保存されるの?

タグ・設定・単語カウントは、**すべて自分のパソコンのChromeの中**(chrome.storage)に保存されます。外部のサーバーには何も送信しません。ツイートの内容も外に出ません。

### 壊れにくくする工夫

Xは見た目のデザイン(クラス名)を頻繁に変えるため、それを頼りにすると拡張機能がすぐ壊れます。X TaggerはX自身がテスト用に付けている `data-testid` という目印(変更頻度が低い)を頼りにしています。それでもXの大規模な改修で動かなくなる可能性はあります。

---

## 4. ファイル構成

| ファイル | 役割 |
|---|---|
| `manifest.json` | 拡張機能の身分証明書。名前・バージョン・どのサイトで動くか等を宣言 |
| `content.js` | 本体。x.comのページ内で動き、ツイートの検査・チップ貼り・広告隠し・単語カウントを行う |
| `background.js` | セット別の集計保存・自動タグ化・リセットを順番に処理し、複数タブの上書きを防ぐ |
| `statistics.js` | 集計形式・タグ条件の比較・バックグラウンドへの通信を共有 |
| `content.css` | チップやハイライトの見た目(色・形)の定義 |
| `popup.html` | ツールバーから開く設定画面のレイアウト |
| `popup.js` | 設定画面の動作(タグの追加・削除・編集、設定の保存) |
| `README.md` | 開発者向けの説明書 |
| `SPEC.md` / `spec.html` | この仕様書 |

---

## 5. 開発の経緯(直したこと)

1. **最初のバージョン**: 広告非表示 + 単語タグ + 頻出単語カウントを実装
2. **タグのオブジェクト化**: 「Python/パイソン/python3が別チップになる」「除外語が書けない」問題を解決するため、タグを「表示名+同義語+除外語」のセットに変更
3. **単語境界の判定**: 英数字の単語が別の単語の一部に誤反応する問題を修正(`\b`付き正規表現)
4. **重複カウントの修正**: スクロールの往復で同じツイートが何度も数えられる問題を、ツイート固有URLの記録で解決
5. **ハイライト消失の修正**: 当初は青い縦線を重ねて対応。その後、縦線を廃止し、タグチップと薄い背景色で表示
6. **フィルタモード追加**: タグに一致するツイートだけを表示するモードを、ホームTL限定・初期OFFで追加
7. **広告判定の強化と英単語の前方一致化**: 広告ラベルの表記ゆれ(広告/Ad等)に対応。英単語の判定を前方一致に変更し、「run」で running / runner も拾えるように(不要なヒットは除外語で対処する運用)
8. **頻出単語を名詞のみに**: ひらがなを含む語を除外し、動詞・形容詞・助詞がカウントされないように
9. **UI刷新とタグセット機能**: ポップアップをカード型レイアウト+トグルスイッチに刷新。タグ一覧を複数の「セット」として保存し、ワンクリックで切り替えられる機能を追加
10. **AI機能の追加**: Chrome内蔵AI(Gemini Nano)による同義語・除外語の候補生成と、頻出単語のノイズ除去を追加
11. **頻出単語をタグ関連ツイートに限定**: 集計対象を「登録タグにヒットしたツイート」だけに絞り、興味の周辺にある関連ワードが浮かび上がるように変更(タグと無関係なツイートの単語は混ぜない)
12. **集計の分離とリセット修正**: 集計・自動タグ・除外リストをセット別に保存。タグ条件変更で再集計し、リセット前のタブから遅れて届く保存は破棄
13. **AIノイズ除去の操作**: 実行ボタンを押すとAIが選んだ語をまとめて除外し、結果を表示
14. **頻出語から集計範囲を拡大**: 登録・昇格した語だけに一致する投稿も表示・集計し、そこから次の関連語を見つける

## 6. 既知の制限

- ページをリロードすると「数えたツイート」の記録が消えるため、同じツイートが再カウントされることがある(カウントは傾向を見る目的なので許容)
- フィルタモードでタグのヒット率が低いと、画面がほぼ空になる
- Xの大規模なデザイン変更で動かなくなる可能性がある(`data-testid` の変更)
- スマホのChromeでは拡張機能自体が使えない(Android: Firefox / Kiwi Browser で代用可)
